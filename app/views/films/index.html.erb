<ul class="content">
	<%= render :partial => "film", :collection => @films %>
</ul>

<%= form_for :film, :url => { :action => "create" }, :remote => true do |f| %>
	<%= f.text_field :title %>
	<%= f.submit %>
<% end %>
